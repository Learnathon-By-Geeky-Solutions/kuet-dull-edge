export enum MaterialType {}

export enum MaterialOwner {
  user = 'user',
  classroom = 'classroom'
}

export enum MaterialStatus {
  active1 = 'active',
  archived = 'archived',
  removed = 'removed'
}
